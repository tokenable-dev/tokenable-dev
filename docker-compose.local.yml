# 로컬 개발 전용 — backend/.env 로드
#   docker compose -f docker-compose.yml -f docker-compose.local.yml up
services:
  backend:
    env_file:
      - ./backend/.env
    environment:
      # backend/.env is host-oriented (127.0.0.1); override for in-network Redis.
      REDIS_URL: redis://redis:6379
