/**
 * Empty stub for Privy optional peer dependencies we do not use
 * (Farcaster mini-app, Abstract, Stripe crypto, permissionless account abstraction).
 * Privy dynamically imports these; aliasing avoids noisy "module not found" warnings.
 */
export {};
