/**
 * Top Cards product UI — set false while styling; backend top100 ingest/API unchanged.
 * Re-enable when the Markets Top Cards section is ready to ship.
 */
export const TOP_CARDS_UI_ENABLED = false;

/** User-facing Top 100 section title (ranked by 90-day sales). */
export const TOP_CARDS_SECTION_TITLE = "Top Cards by Sales (Last 90 days)";

/** Phase 7 — public Markets UI off; preview at /marketplace/admin/top-movers. */
export const TOP_MOVERS_UI_ENABLED = false;
export const TOP_MOVERS_SECTION_TITLE = "Top Movers (Weekly Gain)";
export const TOP_MOVERS_PREVIEW_COUNT = 5;
export const TOP_MOVERS_FETCH_COUNT = 20;

/** Short label for breadcrumbs and back links. */
export const TOP_CARDS_SHORT_LABEL = "Top cards";
