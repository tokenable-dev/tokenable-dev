# Frontend (Next.js)

```bash
pnpm install
pnpm dev
```

- App: [http://localhost:3000](http://localhost:3000)
- Env: copy `.env.example` to `.env.local` if present; set `NEXT_PUBLIC_*` contract addresses and API URL.

Project docs: **[../docs/DEVELOPMENT.md](../docs/DEVELOPMENT.md)**
