"use client";

import { PortfolioPageView } from "@/components/portfolio/PortfolioPageView";

/** Full partner portfolio — same holdings UI as `/portfolio` with Partner-Portfolio.html chrome. */
export default function PartnerPortfolioPage() {
  return <PortfolioPageView variant="partner" />;
}
