import type { ReactNode } from "react";
import "@/styles/tokenable-admin.css";

/** Dev showcases may render admin chrome samples. */
export default function DevLayout({ children }: { children: ReactNode }) {
  return children;
}
