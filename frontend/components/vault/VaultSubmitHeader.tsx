export function VaultSubmitHeader() {
  return (
    <header className="vault-submit-header">
      <p className="vault-submit-header__eyebrow">Submit for vault</p>
      <h1 className="vault-submit-header__title">Tokenize your graded card</h1>
      <p className="vault-submit-header__subtitle">
        Verify your PSA slab, mint an on-chain token, and list on Tokenable — without
        shipping for every trade.
      </p>
    </header>
  );
}
